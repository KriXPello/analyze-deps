# analyze-deps

CLI wrapper around `pnpm why` for analyzing package dependencies.

## Install

```bash
git clone https://github.com/KriXPello/analyze-deps.git
cd analyze-deps
npm ci
npm run setup
```

## Usage

```bash
analyze-deps <pkg1> [<pkg2> ...]
```
