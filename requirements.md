# Проект

CLI утилита на node.js. Является оберткой над `pnpm why` и нужна чтобы упростить вывод команды, а также соотнести пакеты с теми, которые есть в package.json директории, в которой вызывается.

Это временный проект, код из него будет извлечен в монорепозиторий, в котором уже есть typescript и сборщик, поэтому
их не нужно добавлять в dependencies. Запуск для проверки осуществлять через глобально установленный пакет `tsx`


# Примеры использования

Предположим в некотором package.json есть зависимости (порядок важен):
- A@1.0.1 -> D@5.0.0 -> C@1.2.0
- B@2.0.0 -> C@1.2.0
- E@3.0.2 -> F@4.0.0
- G@2.0.1 -> D@6.0.0
- U@0.5.0 -> D@6.0.0
- K@1.1.0 -> Y@3.0.2 -> G@2.0.1
- L@1.2.0 -> G@2.0.1

> Стрелками указаны зависимости пакета, т.е. A -> B значит A зависит от B

Для вызова `analyze-deps C@1.2.0`, вывод:

```
Package: C@1.2.0
A@1.0.1
B@2.0.0
```

Для вызова `analyze-deps F@4.0.0 C@1.2.0` вывод:

```
Package: F@4.0.0
E@3.0.2

Package: C@1.2.0
A@1.0.1
B@2.0.0
```

Для вызова `analyze-deps D` вывод:

```
Package: D@5.0.0
A@1.0.1

Package: D@6.0.0
G@2.0.1
▲
├──K@1.1.0
└──L@1.2.0
U@0.5.0

// Обратить внимание, что K@1.1.0 и L@1.2.0 не выводятся как отдельные пункты, а Y@3.0.2 опускается т.к. его нет в package.json
```

Маркер `▲` выводится **каждый раз** после узла, **только** если у этого узла есть хотя бы один ребёнок:
```
<root1>@<ver1>
▲
├──<child1>@<ver>
│  ▲
│  └──<grandchild1>@<ver>
└──<child2>@<ver>
<root2>@<ver2>
```


То есть для каждой переданной версии пакета (если версия не указана, определить список версий через `pnpm why`) нужно найти пакеты, которые прямо или транзитивно используют данную версию и есть в package.json. Если между зависимостями на выходе есть связь, она должна быть отображена с помощью "дерева", как в примере для `D@6.0.0` (оформить можно с помощью символов типа ├ └ и т.п.)

# Edge cases

## multiple paths

Если в package.json есть пакеты X, A, B и:
- X -> A -> D
- X -> B -> D

То для `analyze-deps D` вывести:
```
Package: D@version
A@version
▲
└──X@version
B@version
▲
└──X@version
```
